✨ **наглядная таблица 64-битных регистров x86-64** (System V AMD64 ABI, Linux)

---

## 📊 Таблица 64-битных регистров x86-64 (общее назначение)

| Регистр | Назначение    | Caller/Callee    | Комментарий                                                          |
| ------- | ------------- | ---------------- | -------------------------------------------------------------------- |
| `rax`   | Аккумулятор   | **Caller-saved** | Результаты функций, временные вычисления                             |
| `rbx`   | Базовый       | **Callee-saved** | Используется для хранения данных между вызовами                      |
| `rcx`   | Счетчик       | **Caller-saved** | Аргумент функции (4-й), loop-инструкции                              |
| `rdx`   | Данные        | **Caller-saved** | Аргумент функции (3-й), I/O                                          |
| `rsi`   | Source        | **Caller-saved** | Аргумент функции (2-й), источник копирования                         |
| `rdi`   | Destination   | **Caller-saved** | Аргумент функции (1-й), назначение копирования                       |
| `rbp`   | Base pointer  | **Callee-saved** | Указатель на стековый фрейм                                          |
| `rsp`   | Stack pointer | —                | Указатель на вершину стека, **никогда не трогать напрямую без нужды** |
| `r8`    | Аргумент 5    | **Caller-saved** | 5-й аргумент функции                                                 |
| `r9`    | Аргумент 6    | **Caller-saved** | 6-й аргумент функции                                                 |
| `r10`   | Временный     | **Caller-saved** | Часто используется внутри системных вызовов                          |
| `r11`   | Временный     | **Caller-saved** | То же самое — временные данные, scratch register                     |
| `r12`   | Свободный     | **Callee-saved** | Можно использовать, но нужно сохранять/восстанавливать           |
| `r13`   | Свободный     | **Callee-saved** | То же                                                                |
| `r14`   | Свободный     | **Callee-saved** | То же                                                                |
| `r15`   | Свободный     | **Callee-saved** | То же                                                                |

---

## 🧠 Как запомнить:

* 📤 **Caller-saved («вызывающий сохраняет»)** — если **ты вызываешь** функцию и хочешь сохранить значение — **сохрани сам** (`push`, потом `pop`).
* 📥 **Callee-saved («вызываемая сохраняет»)** — если **тебя вызвали**, ты должен сохранить эти регистры сам, если хочешь их использовать.

---

### 📌 Резюме:

| Кто сохраняет?                                | Какие регистры?                             | Когда?                                      |
| --------------------------------------------- | ------------------------------------------- | ------------------------------------------- |
| **Ты вызываешь функцию** → **ты сохраняешь**  | `rax`, `rcx`, `rdx`, `rsi`, `rdi`, `r8–r11` | Если хочешь сохранить значение после вызова |
| **Ты реализуешь функцию** → **ты сохраняешь** | `rbx`, `rbp`, `r12–r15`                     | Если ты их используешь внутри своей функции |

---

## 🔧 Специальные регистры:

| Регистр                        | Назначение          | Комментарий                                                 |
| ------------------------------ | ------------------- | ----------------------------------------------------------- |
| `rip`                          | Instruction Pointer | Автоматически указывает на текущую инструкцию               |
| `eflags`                       | Флаги CPU           | Устанавливаются после операций (`cmp`, `test`, `add` и др.) |
| `al`, `ah`, `ax`, `eax`, `rax` | Части `rax`         | Младшие/старшие части регистра `rax`                        |
| `cl`, `ch`, `cx`, `ecx`, `rcx` | Части `rcx`         | То же — доступ к 8/16/32-битным частям                      |

---

## 📦 Пример: Части регистра `rax`

| Имя регистра | Размер | Комментарий           |
| ------------ | ------ | --------------------- |
| `al`         | 8 бит  | младший байт `rax`    |
| `ax`         | 16 бит | младшие 2 байта `rax` |
| `eax`        | 32 бит | младшие 4 байта `rax` |
| `rax`        | 64 бит | полный регистр        |

---

## 📌 На заметку:

* В системных вызовах (`syscall`) аргументы передаются так:
  `rdi` → `rsi` → `rdx` → `r10` → `r8` → `r9`
* Результат обычно в `rax`
* Ошибки в системных вызовах также пишутся в `rax` как **отрицательное число**

---

### **Таблица регистров x86-64 (байтовые, словные и двойные слова)**
В архитектуре x86-64 регистры общего назначения (RAX, RBX, RCX, RDX, RSI, RDI, RBP, RSP, R8-R15) могут адресоваться **по частям** (байты, слова, двойные слова).  

---

## **1. Основные регистры и их части**
| 64-бит (QWORD) | 32-бит (DWORD) | 16-бит (WORD) | 8-бит (BYTE) | Описание                     |
|----------------|----------------|---------------|--------------|------------------------------|
| **RAX**        | **EAX**        | **AX**        | **AL**       | Аккумулятор (возврат значений) |
|                |                |               | **AH**       | Старший байт AX              |
| **RBX**        | **EBX**        | **BX**        | **BL**       | Базовый регистр              |
|                |                |               | **BH**       | Старший байт BX              |
| **RCX**        | **ECX**        | **CX**        | **CL**       | Счётчик (для циклов)         |
|                |                |               | **CH**       | Старший байт CX              |
| **RDX**        | **EDX**        | **DX**        | **DL**       | Данные (исп. в операциях ввода/вывода) |
|                |                |               | **DH**       | Старший байт DX              |
| **RSI**        | **ESI**        | **SI**        | **SIL**      | Указатель источника (для строковых операций) |
| **RDI**        | **EDI**        | **DI**        | **DIL**      | Указатель назначения (для строковых операций) |
| **RBP**        | **EBP**        | **BP**        | **BPL**      | Указатель базы стека         |
| **RSP**        | **ESP**        | **SP**        | **SPL**      | Указатель вершины стека      |
| **R8**         | **R8D**        | **R8W**       | **R8B**      | Регистры общего назначения R8-R15 |
| **...**        | **...**        | **...**       | **...**      |                              |
| **R15**        | **R15D**       | **R15W**      | **R15B**     |                              |

---

## **2. Особенности**
### **① Регистры R8-R15**
- **Нет разделения на старший/младший байт** (только `R8B`, `R9B` и т.д.).  
- Пример:  
  ```asm
  mov r8b, 0xAA   ; Записать байт в младшую часть R8
  ```

### **② Регистры для строковых операций**
- **RSI/RDI** (и их части) часто используются в командах `movsb`, `lodsb`, `stosb`.  
- Пример:  
  ```asm
  movsb  ; Копирует байт из [rsi] в [rdi], затем инкрементирует оба
  ```

### **③ Флаги и управление**
- **Регистр флагов (RFLAGS)** не имеет байтовых аналогов.  
- Для работы с битами используются `BT`, `BTS`, `BTR`.  

---

## **3. Примеры использования**
### **① Запись в разные части регистра**
```asm
mov rax, 0x1122334455667788  ; RAX = 0x1122334455667788
mov al,  0x99                ; RAX = 0x1122334455667799 (изменился AL)
mov ah,  0xAA                ; RAX = 0x112233445566AA99 (изменился AH)
mov ax,  0xBBBB              ; RAX = 0x112233445566BBBB (изменился AX)
mov eax, 0xCCCCDDDD          ; RAX = 0x00000000CCCCDDDD (обнулились старшие биты!)
```

### **② Использование в операциях**
```asm
mov  dl, 0x41       ; DL = 'A' (ASCII)
mov  [rdi], dl      ; Записать 'A' в память по адресу RDI
inc  dil            ; Увеличить младший байт RDI
```

---

## **4. Важные нюансы**
- **При записи в 32-битную часть регистра (например, `EAX`) старшие 32 бита обнуляются!**  
  ```asm
  mov rax, 0x1122334455667788
  mov eax, 0xAAAAAAAA         ; RAX теперь = 0x00000000AAAAAAAA
  ```
- **8-битные операции (`AL`, `BL`, ...) не влияют на остальные биты регистра.**  

---

Структурированная таблица **Caller-saved** и **Callee-saved** регистров x86-64 с краткими пояснениями:

---

### **Регистры x86-64 (64-битные)**
| **Категория**       | **Регистры**      | **Описание**                                                                 |
|----------------------|-------------------|-----------------------------------------------------------------------------|
| **Caller-saved**     | `rax`, `rcx`, `rdx`, `rsi`, `rdi`, `r8`, `r9`, `r10`, `r11` | Если функция хочет сохранить их значения — должна сама сохранить перед вызовом другой функции. **Используются для аргументов и временных данных.** |
| **Callee-saved**     | `rbx`, `rbp`, `r12`, `r13`, `r14`, `r15`                    | Вызываемая функция обязана сохранить и восстановить их значения перед возвратом. **Используются для долгоживущих данных.** |
| **Специальные**      | `rsp` (указатель стека)                                     | Нельзя изменять произвольно! Только через `push`/`pop` или `call`/`ret`. |

---

### **Детализация**
#### **1. Caller-saved (Volatile)**
| Регистр | Типичное использование                          |
|---------|-----------------------------------------------|
| `rax`   | Возвращаемое значение функции                 |
| `rcx`   | 4-й аргумент, счётчик циклов (`loop`)         |
| `rdx`   | 3-й аргумент, данные для операций ввода/вывода |
| `rsi`   | 2-й аргумент, источник данных (например, `memcpy`) |
| `rdi`   | 1-й аргумент, назначение данных               |
| `r8-r9` | 5-й и 6-й аргументы функций                   |
| `r10-r11` | Временные данные, часто используются в системных вызовах |

#### **2. Callee-saved (Non-volatile)**
| Регистр | Типичное использование                          |
|---------|-----------------------------------------------|
| `rbx`   | Хранение данных между вызовами функций        |
| `rbp`   | Указатель на базовый стековый фрейм (опционально) |
| `r12-r15` | Долгоживущие данные, которые должны сохраняться |

---

### **Правила использования**
- **Caller-saved**:  
  Если ваша функция использует эти регистры — сохраняйте их в стеке **перед** вызовом другой функции (если хотите сохранить значения).  
  Пример:
  ```asm
  push rcx  ; Сохранили rcx
  call some_function
  pop rcx   ; Восстановили
  ```

- **Callee-saved**:  
  Если ваша функция изменяет эти регистры — обязана восстановить их **перед** возвратом (`ret`).  
  Пример:
  ```asm
  my_function:
    push rbx     ; Сохранили
    mov rbx, 123 ; Используем
    ... 
    pop rbx      ; Восстановили
    ret
  ```

---

### **Особые случаи**
- **`rsp`**: Никогда не изменяйте напрямую (кроме пролога/эпилога функции).  
- **`rax`**: Всегда содержит возвращаемое значение (если оно есть).  
- **`rbp`**: В современных компиляторах часто не используется (оптимизации), но по соглашению — Callee-saved.  

Таблица охватывает все 16 основных 64-битных регистров. Для 32/16/8-битных версий (например, `eax`, `ax`, `al`) правила наследуются от соответствующих 64-битных регистров.
